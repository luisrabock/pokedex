import React from "react";
import { render } from "@testing-library/react";

import ImageAvatars from "../../../components/ImageAvatars";

describe("imageAvatars tests", () => {
  it("renders App component", () => {
    render(<ImageAvatars />);
  });
});
